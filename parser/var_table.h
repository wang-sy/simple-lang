#pragma once

#include "ast/ast.h"

#include <map>
#include <string>

using namespace std;

// VarTable is a class used to store all variables.
class VarTable {
public:
    VarTable();
    ~VarTable();
private:
    map<string, ast::>
};